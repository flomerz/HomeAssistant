substitutions:
  name: livingroom-tv


esphome:
  name: ${name}

esp8266:
  board: esp8285

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Livingroom-Tv Fallback Hotspot"
    password: "BBSgb3yug4Rq"

captive_portal:

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "Hd8tOAj1/8gHU0P/fW7OXFtyGnWZKJufa0bzfe9W3z8="

ota:
  - platform: esphome
    password: "dedf65b4a19d99cfabb6b6ec39723081"

binary_sensor:
  - platform: gpio
    id: button
    name: ${name}_button
    pin:
      number: GPIO0
      mode:
        input: true
        pullup: true
      inverted: true
    on_press:
      - switch.toggle: relay

switch:
  - platform: gpio
    id: relay
    name: ${name}_relay
    pin: GPIO12

status_led:
  pin:
    number: GPIO13
    inverted: yes
